# Nixpacks configuration for Railway
[phases.setup]
nixPkgs = ['python311', 'postgresql', 'libpq']

[phases.install]
cmds = [
  'pip install --upgrade pip',
  'cd backend && pip install -r requirements.txt'
]

[start]
cmd = 'cd backend && python3 start.py'
